import { env } from "../../config/env";

// Mirrors the frontend's Dark Gaming/Tech theme (app/globals.css) — dark background, gold + blue neon accents.
export const emailColors = {
  bg: "#121212",
  card: "#1c1c1c",
  border: "#2e2e2e",
  gold: "#f5b700",
  blue: "#2f8fff",
  text: "#f5f5f5",
  muted: "#a3a3a3",
};

export const FRONTEND_URL = env.frontendUrl;
